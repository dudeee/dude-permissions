{
  "name": "dude-permissions",
  "description": "Trigger `listen` only of the requesting user has permission",
  "version": "1.1.8",
  "main": "build/index.js",
  "repository": {
    "type": "git",
    "url": "https://github.com/mdibaiee/dude-permissions"
  },
  "config": {
    "ghooks": {
      "pre-commit": "grunt; npm run lint"
    }
  },
  "scripts": {
    "lint": "eslint src test",
    "lint-fix": "eslint src test --fix",
    "test": "echo \"Error: no test specified\" && exit 1",
    "lint": "eslint src"
  },
  "author": "Mahdi Dibaiee <mdibaiee@aol.com> (http://dibaiee.ir/)",
  "license": "MIT",
  "dependencies": {
    "babel": "5.8.3",
    "grunt": "0.4.5",
    "grunt-babel": "5.0.3",
    "grunt-contrib-watch": "0.6.1"
  },
  "devDependencies": {
    "eslint": "2.10.2",
    "eslint-config-pichak": "1.0.0",
    "ghooks": "1.1.1",
    "grunt-contrib-clean": "1.0.0"
  }
}
